// && -> AND Gate
// || -> OR Gate

let a = true;
let b = false;
console.log(a && b); //  AND
console.log(a || b); //  OR
console.log(!a); // 

console.log(5 != "g");  // Value 5, g ! that true, but type is different, so true
console.log(5 != "5");  // Value 5, 5 ! that false, but type is different, so false
console.log(5 !== "5"); // Value 5, 5 ! that false, and type is different, so true